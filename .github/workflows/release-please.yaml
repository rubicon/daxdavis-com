name: release-please

on:
  push:
    branches: [main]

permissions: {}

jobs:
  release-please:
    name: Propose release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
    steps:
      # Item is addressed by UUID, not title: a title-pinned reference breaks
      # silently the moment the item is renamed.
      - name: Load release-please App credentials from 1Password
        id: op
        uses: 1password/load-secrets-action@e544b780808654ba8ceba5fb2fe2897103d92ff4 # v5
        with:
          export-env: false
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          RP_CLIENT_ID: "op://Automation/lnacky4l2tmlihrockbutkub7y/client id"
          RP_PRIVATE_KEY: "op://Automation/lnacky4l2tmlihrockbutkub7y/private key"

      - name: Mint GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          client-id: ${{ steps.op.outputs.RP_CLIENT_ID }}
          private-key: ${{ steps.op.outputs.RP_PRIVATE_KEY }}

      - name: Run release-please
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        with:
          token: ${{ steps.app-token.outputs.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
